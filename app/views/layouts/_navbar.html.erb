  <nav class="navbar navbar-expand-lg bg-light sticky-top bg-white border-bottom  " style="border-color: #afafaf">
  <div class="container-fluid w-75 ">
    
    <!-- logo -->
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag 'logo.png', width: "100rem" %>
    <% end %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

    <div class="d-flex"> 
      <!-- search -->
      <div class="collapse navbar-collapse me-5" id="navbarSupportedContent">
        <form class="d-flex" role="search">
          <input class="form-control me-2 border-0 " type="search" placeholder="Search" aria-label="Search" style="background: #efefef">
        </form>
      </div>
      
      <!-- icons -->
      <div class="navbar-collapse gap-4 ">
        
        <%= link_to root_path, class: "text-dark" do %> <i class="fa-solid fa-house fa-xl "></i> <% end %>  
        
        <i class="fa-brands fa-facebook-messenger fa-xl" ></i>

        <!-- create new post -->
        <a type="button" class="text-dark" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
          <i class="fa-regular fa-square-plus fa-xl"></i>
        </a>
        
        <i class="fa-regular fa-compass fa-xl"></i> 
        <i class="fa-sharp fa-regular fa-heart fa-xl"></i> 

      <!-- dropdown -->
      <div class="dropdown">
        <a class="" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          <% if current_user && current_user.profile_pic.attached? %>
            <%= image_tag  current_user.profile_pic, class: "img-thumbnail rounded-circle", style: "width: 15%" %>

          <% else %>
            <%= image_tag  'user-pp.png', class: "img-thumbnail rounded-circle", style: "width: 15%" %>
          <% end %>  
        </a>
        <ul class="dropdown-menu">
          <li> <%= button_to 'Log out', destroy_user_session_path, class: "dropdown-item", method: :delete %> </li>
        </ul>
      </div> 
        
        <!-- user_image -->
        <div>  
          <% if current_user && current_user.profile_pic.attached? %>
            <%= image_tag  current_user.profile_pic, class: "img-thumbnail rounded-circle", style: "width: 15%" %>

          <% else %>
            <%= image_tag  'user-pp.png', class: "img-thumbnail rounded-circle", style: "width: 15%" %>
          <% end %>  
        </div>

        

      </div>
    </div>
  </div>
</nav>

<%= render 'posts/form', post: Post.new %>

<!-- to open this model, the form or the model  itself need to be within the same page of this button  -->
<!-- if we are in the home page or the user page or any other page the model won't open  -->
<!--  the model to be open the form any pages the user is currently in  -->
<!-- we can render this form partial down  -->